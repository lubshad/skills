---
name: frontend-strategy
description: Use when legacy references mention frontend-strategy or when bridging old frontend guidance to current frontend-* skills.
---

Compatibility adapter.

For frontend platform strategy and UI interaction rules, read:

- `frontend-interaction-patterns.md`
- `frontend-admin-panels.md` when the surface is admin/back-office
- The relevant platform implementation skill (`flutter-*`, `nextjs-*`, or `frappe-*`)

Do not add new UI behavior rules here. Put platform-agnostic UI/product behavior in `frontend-*` source-of-truth skills and platform mechanics in platform-specific skills.
